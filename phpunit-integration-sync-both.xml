<?xml version="1.0"?>
<phpunit
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.6/phpunit.xsd"
  bootstrap="./tests/integration/bootstrap.php"
  colors="true"
  convertErrorsToExceptions="true"
  convertNoticesToExceptions="true"
  convertWarningsToExceptions="true"
  convertDeprecationsToExceptions="true"
  >
  <testsuites>
    <testsuite name="Safe Publish Sync Both Integration Tests">
      <directory suffix=".php">tests/integration/sync-both/</directory>
    </testsuite>
  </testsuites>

  <php>
    <!-- Boot the plugin in "both" sync mode (send and receive active simultaneously). -->
    <env name="WP_TEST_SYNC_MODE" value="both" />

    <!-- Point wp-phpunit to our test config file -->
    <env name="WP_PHPUNIT__TESTS_CONFIG" value="tests/wp-tests-config.php" />

    <!-- Point to wp-phpunit installation in vendor -->
    <env name="WP_PHPUNIT__DIR" value="vendor/wp-phpunit/wp-phpunit" />

    <!-- Database configuration (wp-tests-config.php reads these) -->
    <env name="WP_DB_NAME" value="tests-wordpress" />
    <env name="WP_DB_USER" value="root" />
    <env name="WP_DB_PASSWORD" value="password" />
    <env name="WP_DB_HOST" value="mysql" />
  </php>
</phpunit>
